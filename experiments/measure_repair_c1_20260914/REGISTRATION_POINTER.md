# MAHAKAL_C1_FRESH_CALIBRATION_20260914: actual pre-outcome registration

This commit registers the actual C1 implementation, independent checker, configuration, environment, complete protocol and thresholds BEFORE any C1 numerical, model, data, fitting or calibration outcomes. Only AST parsing and input/byte integrity checks preceded registration. The scope is the Mahakal photon-ring movie-recovery initiative, not CovQ/QFIC. Parent: 2c4a6b8cc0d8090561b6b27fd6e432c5d28618a3.

Concatenate source_archive/SOURCE_SNAPSHOT.b64.00 through .07 in numeric order and base64-decode to gzip tar. Exact decoded archive: 22543 bytes, SHA-256 114f1b610a710184ee1d2eacf4e5a344defb7992302a03f7cbd49df3213a04eb. It contains the complete REGISTRATION.md, six Python source files, config.json, requirements.txt, ENVIRONMENT.json, README.md and FREEZE.json. FREEZE.json is 2343 bytes, SHA-256 584f0c12335f9a81c29bcab82a9810ed41454ac28facc41b409c3378c79a996a, Git blob 0eeec5ff99859ee49bbfe4f055e492b2ed917cfb. It binds every one of the eleven source/configuration/document members by exact size, SHA-256 and Git blob. Read the complete archived REGISTRATION.md before executing. This pointer is not a substitute for its probability laws or gates.

Predeclared campaign: 128 fresh primary cases per dimension (8D and 16D), eight extra cancellation controls per dimension, a second independent inference replicate on the first 16 primary cases per dimension. This gives 272 independent datasets (256 primary), 608 method-case runs, 1216 family-stratum runs. Both unknown families are inferred on every case; inference never receives the true family or source. An all-coordinate nonlinear measure-preserving transform supplies an independently solvable reference. Its tractability and synthetic scope are explicit limitations.

Compare PRIOR_RW and FITTED_BRIDGE at N=512. The latter actually fits a full-covariance defensive Student proposal on a data-only surrogate pilot, freezes it, and starts an independent main proposal-to-surrogate-to-native SMC population. No clipping. Both methods use exactly 29696 native AND 29696 surrogate evaluations per family including pilot/initialization/rejected candidates. Costs are matched by evaluation kind, not wall time or all arithmetic. Both run a fixed schedule. Independent replay covers proposals, RNG states, mixture draws, fits, acceptance, ancestry, incremental evidence and final weights.

Registered checks include actual-kernel stationarity, fitted sample-versus-density tests, independent references, all-coordinate means/covariances/CDFs, family odds, data-dependent randomized SBC, marginal 90/95 intervals and widths, joint family-parameter ellipsoids and coverage, independent-case uncertainty intervals, replication and cancellation. Every numerical/statistical gate is fixed in the archived source and protocol. Complete the whole population after a statistical failure; stop on correctness/reference/budget failure. No thresholds, seeds or sources may change under this identity after outcomes.

Execution requires an immutable readback of all chunk blob identities, archive/member hashes and a registration receipt bound to this commit. No C1 outputs exist at this registration. A local receipt alone is not remote authentication. Full raw tape availability must be distinguished from compact remote records.

Paper I remains protected at research/mahakal_v4_1, commit 084fb45fedae99f203393dbc1e9cdda9ab250c2d; zero writes or units. 006R sampler adequacy remains withdrawn and FAMILY_EVIDENCE_BRIDGE_007 remains HOLD. No rays, physical observations, movie fits, physical posterior release or lost Movie009-R2 reconstruction is authorized. Even a C1 reference-calibration pass requires a separate physical C2 registration.
