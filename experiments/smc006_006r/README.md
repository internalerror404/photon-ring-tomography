# Mahakal Paper II — SMC006 / SMC006R canonical completion deposit

This additive branch records two inference-algorithm experiments after `KNOT_QUADRATURE_002`:

- `WITHIN_FAMILY_SLICE_SMC_006`: preserved incomplete execution; its 11 completed subsets are not promoted.
- `WITHIN_FAMILY_SLICE_SMC_006R`: fresh corrected execution; all 128 datasets and 480 method-case runs completed, but the registered physical-release gate failed.

The scientific result is narrow. Slice mutation roughly halved the primary 8D within-family posterior-mean error relative to the parent random-walk mutation. Family-probability calibration, evidence estimates, and independent-run agreement still failed. No photon-ring data, movie endpoint, Kerr integration, or Paper-I unit was generated.

The GitHub JSON records are canonical for disposition and headline metrics. Their source-and-record hash tables authenticate the full local source/results package. The exact complete binary particle banks and 5,329-file artifact package are delivered separately and are not claimed to be repository blobs.
