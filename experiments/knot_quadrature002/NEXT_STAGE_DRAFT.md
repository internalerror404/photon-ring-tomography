# Next scientific stage - design constraints, not an executable registration

This document does not authorize or report a movie or posterior run. The occupied Movie009-R3 lineages must remain distinct. Select a fresh successor identity after reconciling their registrations/completions and later branches.

The numerical stage is necessary for its specified finite representation; it is not by itself a solution of historical recovery. Its q8/q12 rules are composite rules at identical physical pixels, with CACHE001 source norms, noise and support held fixed. Nothing here authenticates the missing original R2 implementation or reproduces its numerical outcomes.

The scientific successor must implement a normalized joint distribution over unknown background family/parameters and historical family/parameters or field. Retain within-basin and between-basin uncertainty, not merely likelihood weights over a few optimized minima. Count the direct likelihood once, use correct prior/proposal/evidence factors, and retain all failures. Direct-identical observations must use identical inference mechanics (including deterministic or coupled computational randomness); arbitrary case identifiers must not carry sibling labels.

A useful sequence is a fully specified continuous analytic-family sampler prototype, independent simulation-based algorithm checks, then a separate held-out calibration/test population. A finite family library is not arbitrary off-family recovery. Radial plume excluded from tuning but present in the inference library is held-out-from-tuning, not an unseen model class.

Required comparisons are a matched-representation single-best/multistart estimator, joint posterior primary readout fixed prospectively, direct-only and labelled arms with exactly paired direct noise, a truth-background oracle restricted to diagnostics, and an order/timing negative control. A reconstruction from an oracle cannot pass a deployable-method gate. Reuse of an already examined population is postplanned mechanism evidence, not fresh confirmation.

Before outcomes, fix active parameter dimensions (remove analytically unused nuisance coordinates or integrate their normalized priors), family prior probabilities, physical support/positivity policy, sampler settings and finite budgets, calibration acceptance conditions, source counts/noise streams, exact source and environment, and failure-safe completion checks. No infinite exploration, population selection from detector response, or source/noise replacement after inspection.

The original movie scoring definitions and gates remain: error <=0.35, correlation >=0.75, inactive predicted RMS <=0.05; at least 12M at 95% population reliability and >=8M beyond direct; >=95% labelled individual and >=90% pair-both identification; >=90% labelled differential all-active-frame pass; original chance-level direct controls; positive family improvement including plume. Check actual clean AND fitted q8/q12 fields against relative 5e-4 and whitened 0.1, not just basis columns. Report 90%/95% interval coverage with history-cluster uncertainty, distinguishing pixelwise from simultaneous whole-movie coverage and raw posterior from calibrated intervals.

If a sampler fails to mix or cover multiple modes, that is an algorithmic failure to preserve, not evidence that the physical posterior is unimodal. If intervals are wide, report the uncertainty rather than interpreting mean images as measured movies. Keep measurement-supported and full-annulus endpoints separate. Physical continuum accuracy, realistic visibility-domain acquisition and cross-geometry generalization remain unestablished by this cache experiment.
