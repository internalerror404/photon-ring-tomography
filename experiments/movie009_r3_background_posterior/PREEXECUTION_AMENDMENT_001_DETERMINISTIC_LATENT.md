# Movie009-R3 preexecution amendment 001 — deterministic latent mechanism panel

The registered R3 protocol names the frozen three-seed Movie009-R2 MLP as the primary innovation map. Reconstructing the complete neural training state in the current runtime would require repeating three CPU training runs and introduces runtime-dependent floating-point variation that is unnecessary for the mechanistic question.

Before any R3 posterior hypothesis or endpoint has been evaluated, this amendment makes the validation-selected **latent ridge** the primary background-posterior map. It is deterministic, already an explicit Movie009-R2 method, and differs only in the residual-to-latent estimator; the source PCA, physical operators, background posterior beam, joint refinement, metrics, thresholds, test population, and success gate are unchanged.

The R2 latent-ridge aggregate at SNR0=300 becomes the K=1 replay target: labelled identification 0.99609375, pair-both 0.9921875, span95 6M, span90 8M, median error 0.23390243433573232, and differential pass 0.296875. `TRUE_BACKGROUND_LATENT` likewise uses the deterministic latent-ridge map and therefore isolates background uncertainty from source-PCA representation without neural-training variance.

The MLP posterior panel is deferred, not silently dropped. If deterministic posterior retention materially improves the registered endpoint, an independently frozen neural posterior successor may be run on a fresh population. No source, noise, physical row, beam direction, beam width, displacement, score, metric, or numerical gate changes.