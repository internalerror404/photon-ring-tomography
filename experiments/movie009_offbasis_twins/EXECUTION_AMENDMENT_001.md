# Movie009 execution amendment 001 — compact plateau window

Before any validation/test source was admitted and before any observation or inverse outcome existed, replace the incompatible old-time envelope with a compact C2 plateau window:

- zero for `tau <= -30M` and `tau >= -6M`;
- quintic smoothstep rise on `[-30,-26]M`;
- exactly one on `[-26,-10]M`;
- quintic smoothstep fall on `[-10,-6]M`.

This preserves the load-bearing causal condition: every twin difference is identically zero at all direct-ray source times, whose q12 minimum is approximately `-2.533454M`. It also makes the registered requirement of activity over nine old frames mathematically compatible with the registered feature-amplitude range. Source families, parameter ranges, response-blind admission, population sizes, reconstruction methods, thresholds, seeds, and success gates are unchanged.

The source-independent rich operator and source Gram from the failed launch are retained and rehashed. Source code is re-frozen before generating the first accepted source.
