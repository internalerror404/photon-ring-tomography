# Movie011 execution failure 002 — preparation exceeded the local command envelope

After extending the candidate cap, the second `prepare` attempt exceeded the 20-minute local command envelope before writing a population file. No validation/test bank, clean-response result, neural weight, checkpoint, or scientific endpoint exists from that attempt.

Profiling localized the avoidable cost to repeated Python-level frame and response evaluation during candidate admission and source rendering. The repair vectorizes the identical analytic source formulas across all 15 movie frames and all observer-time/ray coordinates. A pre-outcome equivalence check gives maximum absolute differences exactly 0.0 for both source-grid frames and an q8 physical response on a development source. The candidate RNG stream, admission predicate, source parameters, populations, observations, architecture, thresholds, and endpoints are unchanged.
