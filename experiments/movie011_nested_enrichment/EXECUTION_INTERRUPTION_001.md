# Movie011 execution interruption 001

The first local Movie011 attempt did not produce `SUMMARY.json`, `FAILURE.json`, a source bank, a regularization decision, or any reconstruction endpoint before the tool runtime was reset. The only user-facing status file said that execution failed and contained no scientific result. It is not evidence for either a pass or a fail.

The successor execution is therefore a clean restart from the prospectively registered protocol. It reuses authenticated Movie007 physical arrays, performs exactly zero new Kerr calls, and freezes the actual executable source on GitHub before generating Movie011 sources or outcomes. Thresholds, source populations, nested dimension, and success gates remain as registered.
