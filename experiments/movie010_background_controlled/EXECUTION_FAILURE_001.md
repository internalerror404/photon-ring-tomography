# Movie010 execution failure 001

The first execution completed the source-independent rich operator construction, generated the frozen validation and test source specifications, rendered their clean q8/q12 data, and evaluated every registered validation ridge exponent. It stopped before any test reconstruction because the selected validation record was inserted into its own `scores` list and strict JSON serialization detected that circular Python reference.

The validation decision already exists and is retained: exponent `-2` for direct and `-1` for labelled, selected by the registered minimum pooled median validation support error. No test reconstruction, family endpoint, movie span, or success-gate result existed at failure. The correction copies the selected record before attaching the full score table; it changes no source, observation, candidate score, selection rule, threshold, or estimator. The deterministic source/operator construction is rerun rather than edited around.
