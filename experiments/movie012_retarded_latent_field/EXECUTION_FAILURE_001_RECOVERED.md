# Movie012 recovered execution failure 001 — cached physical fields are separate arrays

The first recovered implementation authenticated every frozen Movie007 input and located the four legacy source-linear operators. It stopped while adapting the q8 direct physical cache, before generating any Movie012 source, PCA basis, observation preprocessing, neural weight, validation choice, or test endpoint.

The adapter assumed that source radius, phase, and retarded time were stored in one combined coordinate tensor. The authenticated archive instead stores the physical ray fields as separate arrays. This is an implementation-interface failure, not a scientific result.

The repair broadens only the cache reader: it assembles coordinates from the stored source-radius, source-phase, delay/source-time, redshift, quadrature, and pixel-area arrays; candidate conventions are accepted only if they reproduce the frozen 595-column q8/q12 legacy operators. No source formula, population, seed, PCA rank, neural architecture, optimizer, metric, threshold, or success gate changes. No physical call or Paper-I unit was used.
