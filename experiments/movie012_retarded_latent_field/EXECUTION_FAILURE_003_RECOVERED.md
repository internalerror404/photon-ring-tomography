# Movie012 recovered execution failure 003 — repaired kernel did not propagate through the returned design container

The full-column kernel readback completed far enough for the outer physical replay to run, but the q8 direct replay still failed at the caller. No Movie012 source population, PCA basis, neural weight, validation choice, or test endpoint was generated.

The recovered wrapper replaced ndarray leaves in tuples, lists, and dictionaries. The inherited adapter may return a dataclass-like physical-design container, so the recovered coordinate/kernel arrays were not guaranteed to propagate through that object boundary. This is an implementation-container failure, not a scientific result.

The repair preserves the exact returned container type while recursively replacing dataclass/object ndarray fields, and emits a pre-outcome kernel-recovery diagnostic containing the full 595-column replay residual. Source families, populations, seeds, PCA ranks, network, optimizer, metrics, thresholds, and success gate remain unchanged. No new physical call or Paper-I unit is used.
