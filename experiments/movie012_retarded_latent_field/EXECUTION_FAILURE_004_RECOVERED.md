# Movie012 recovered execution failure 004 — inherited replay gate aborted before full-column repair

The container-preserving recovered wrapper still did not execute its full-column kernel solve because the inherited cache adapter raised its own q8-direct replay exception before returning the candidate design container. The process again stopped before any Movie012 source, PCA object, neural checkpoint, validation choice, or test endpoint existed.

The repair separates responsibilities. The inherited routine is used only to construct the candidate coordinate/container shape and no longer adjudicates its weak pilot-derived kernel. The recovered wrapper immediately reconstructs the node kernel from all 595 frozen source columns and all 13 observer times, enforces the full replay tolerance, and only then returns the design to the rest of Movie012.

No physical input, source family, population, seed, PCA rank, architecture, optimizer, metric, threshold, or success gate changes. No new physical call or Paper-I unit is used.
