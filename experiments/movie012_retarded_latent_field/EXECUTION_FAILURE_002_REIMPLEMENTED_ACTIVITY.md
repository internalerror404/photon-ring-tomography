# Movie012 reimplementation failure 002 — union-support activity admission does not fill the frozen families

After the Python 3.13 import repair, the preparation attempt ran to the local 30-minute execution cap without writing `PREPARED.npz`, a source-spec file, candidate ledger, PCA, neural weight, or test endpoint. A post-failure geometry-only admission diagnostic—using no order-1 response and no reconstruction—showed the cause: requiring both siblings to exceed source RMS 0.03 on the **union measurement-support mask** for at least nine old frames is effectively infeasible for the recovered narrow-hotspot and split/merge formulas. Across a 1,000-candidate diagnostic, narrow hotspots reached at most seven qualifying frames and split/merge at most eight under the recovered implementation.

This activity predicate was introduced by the reimplementation amendment; it is not specified by the original Movie012 registration, which requires only that source admission not use order-1 response or reconstruction performance. No accepted Movie012 population or scientific outcome exists.

The pre-outcome repair separates source existence from detector coverage: require nine-frame activity under the fixed **full-annulus source norm**, while retaining the registered union-support metric for every primary recovery endpoint. This admits natural historical movies without selecting them for visibility to order 1. The exact same feature formulas, parameter ranges, seeds, positivity rule, physical observations, architecture, metrics, thresholds, and success gate are retained. Corrected source is re-hashed and re-frozen before population generation.
