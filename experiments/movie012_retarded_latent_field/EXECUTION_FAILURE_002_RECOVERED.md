# Movie012 recovered execution failure 002 — pilot-column cache alias

The separate-field cache adapter located a plausible q8 direct coordinate/kernel convention and passed its small pilot-column check, but the mandatory full 595-column operator replay rejected it. The process stopped before generating any Movie012 source population, source PCA, observation PCA, neural weight, validation choice, or test endpoint.

The failure shows that a pilot source-column subset is insufficient to disambiguate the cached ray-field convention. It is an implementation-interface failure, not a scientific result.

The repair preserves the adapter's authenticated coordinate discovery but recovers the static per-pixel quadrature kernel by least squares against **all 595 frozen operator columns across all 13 observer times**, then requires full-operator relative replay error <=2e-8. No physical call is made: the legacy source-linear operator and cached coordinates uniquely determine the finite detector weights for this readback. Source formulas, populations, seeds, PCA ranks, neural architecture, optimizer, metrics, thresholds, and the success gate are unchanged.
