# PAPER_I_FINAL_TEXT_REVIEW_039

Repository: internalerror404/photon-ring-tomography  
Branch: research/mahakal_v4_1  
Reviewed commit: 1a6075512f714ca2b8a52173227b88a73cdad19b  
Disposition: METHODS_SUBSTANTIALLY_ACCEPTED_ARCHIVAL_DEPENDENCIES_NARROWED_RELEASE_PREPARATION_AUTHORIZED

This is an author-directed review, not a journal referee decision or permission
to submit. The central 038 repairs are accepted. Do not reopen the support
statement, saved R2 localization, corrected reach semantics, regime distinctions,
accepted numerical hull/comparator/sample-absence scopes, or the closed negative
template test merely to polish the manuscript.

The next deliverable should be a reader-facing candidate with finished methods,
verified relevant references and corrected figures, not another general rewrite
or a larger declaration of how many schema fields pass. No new physical work is
needed for the source-based tasks identified here.

## 1. What this review actually established

The 038 draft, bibliography/figure status, semantic-pin summary and selected
claim records were read through the GitHub connector. The review followed the
R1 execution manifest to its caller and scorer, and the HMT2 execution manifest
to its scoring caller, endpoint writer and paired_relative helper. The inspected
HMT2 file blobs match at execution commit 9713af2c and the reviewed head.

Seven primary arXiv metadata/abstract pages were separately checked to START the
bibliography audit. Full external papers, all 26 old references, the numerical
parquet payloads, and the complete 230-pin dependency graph were not independently
audited in this review. No campaign ray, physical integral, source bank, estimator,
spectrum or repository suite was executed. An accompanying arithmetic record
illustrates the aggregation distinction without re-scoring any real history.

## 2. Two of the three named method unknowns can now be removed

The phrase 'two dependencies' groups three items: the R1 grid, R1 realized eta,
and HMT2 aggregation order. The grid and aggregation are now source-resolved.

**R1 grid.** The actual R1 runner at its execution commit calls evaluation_grid
with four support-bound arguments and no size overrides. Its imported function
at that same commit specifies n_r=10, n_phi=12 and n_t=40. The paired caller and
callee therefore establish the 4,800-point grid. This is not guessing from a
function default in isolation. The support limits are recorded in the executed
R1 freeze. The exact call, source identities and proposed insertion are provided
in review039/METHOD_DEPENDENCY_CLOSEOUT_039.md. No generated-grid hash is claimed.

**HMT2 aggregation.** Read the caller and helper together: average state errors
over ages for each draw, average those per-draw scores for each history, form a
direct-relative error reduction for each paired history, then take the median
across histories. The endpoint writer uses the helper's median_ci_low, not its
separate cell-balanced-mean interval. Ten thousand median-bootstrap resamples
use helper seed 20260954 (the supplied HMT2 seed plus one). This resolves the
previously unread function-body dependency, not the accuracy of the physical
forward model. Full formulas and source blobs are supplied in the same note.

**R1 eta.** Its rule and code path are known, but the realized scalar was not
located in the inspected freeze or manifest. The freeze has eta_value=null;
the runner computes it from prior-fit values and prints it. Preserve this
precise residual limitation. A bounded search of saved logs/sidecars/fit norms
is authorized; rebuilding truths or replaying the experiment is not. Do not
assert that no copy exists merely because the inspected records lack it.

Use consistent statuses: a method formula can be source-resolved while one
realized scalar remains unlocated. A count of one UNRESOLVED pin is not an
inventory of all scientific or reproducibility dependencies. Link the eta
dependency to affected R1 claims rather than hiding it solely in Appendix A.
This does not make the accepted legacy benchmark a different numerical result.

## 3. One real provenance overgeneralization still remains in the prose

Section 3 says TSVD/ridge were tuned on R0C and frozen before the main bank.
That is the R1 lineage. The HMT2 sealed freeze says it uses its OWN stage-1
selection, inherited unchanged. Replace the global sentence by separate R1 and
HMT2 tuning-lineage statements; no estimator or chosen hyperparameter changes.

The 'noise convention' cell for HMT2 still says only four paired draws, although
the following text correctly separates replication from covariance. Replace that
cell with the actual standard-normal parent-noise/readout convention and put the
draw count in replication metadata. This is a consistency edit, not a new noise
model. The R1 posterior-calibration withdrawal also belongs to its probabilistic
estimator program, not to a new posterior supplied by TSVD/ridge.

Fix the opening and section-11 links that still name CLAIM_EVIDENCE_MATRIX_037.json.
The revised input manifest, matrix, draft and completion report must identify
the same document version. Do not use a schema pass to mark a still-unlocated
eta value or another unresolved implementation input as complete.

The structural caveat should say nonzero columns 'need not be independent or
operationally recoverable', not literally that nonzero columns 'are not'
independent. The point is insufficiency, not impossibility. Retain the theorem
and its nonvanishing assumptions without changing its mathematical scope.

## 4. Finish the requested scientific exposition, without inflating it

Appendix A now correctly distinguishes the R1 scoring matrix, R2 source Gram
and screen quadrature. Keep that distinction. Use the resolved grid and reduction
formulas supplied here to replace the dependency boxes, and leave only the eta
status that has not been recovered.

State source representations and observation conventions rather than leaving
C224/L224 as unexplained identifiers. The existing freeze gives C224 as four
cubic B-splines in log radius, seven real azimuthal modes and eight temporal
DCT modes, and the reference observer times as eight samples from 0 to 20 M.
The compact-support class is a different temporal representation and must keep
its own definition. Likewise the main text should state the age-probe function,
its normalization and the actual SNR convention used for the retained E3C
quantities; a mention of a reference SNR alone does not define the experiment.
These are previously requested exposition items from existing source code and
records, not a new source class or simulation.

Describe each retained claim at its actual scope. The class-dependent rank and
family-dependent morphology tables are now separated correctly. Do not turn wide
family intervals into a blanket statement that no family-level observation can
be reported; the measured point estimates and intervals remain reportable, while
strong universal claims do not follow. Keep the maximum-channel response
criterion and its known-support/fine-reference limitations explicit.

## 5. Literature and figures must now become output, not recurring placeholders

The bibliography work can now say 'core metadata/abstract audit started' rather
than 'not started': review039/CORE_BIBLIOGRAPHY_AUDIT_039.md identifies seven
verified primary records, their narrow uses and exactly what was not read.
Use the records for those narrow background statements, and read the necessary
full-text sections for any stronger claim. Complete the relevant prior-work
account before a submission candidate. A full novelty survey is not claimed by
this review, and an old title/DOI list is not automatically verified literature.

Regenerate the two identified misleading figure assets from authenticated saved
tables into NEW filenames. Their titles and panels, not merely their captions,
need the corrected statistics: oldest detectable probe versus anchor-connected
span, and operational-rank fraction versus numerical rank under mixed spatial/
temporal enrichment. Keep unchanged values unchanged. Derived summaries from
existing tables and document rendering are allowed; no new numerical operator
or reconstruction may be invoked by a figure builder. Run a writer only in a
disposable worktree or redirected output root.

The final candidate should render mathematical notation and contain actual
numbered, cited figure assets. Hash the reader-facing artifact after rendering
and check its pages, equation/figure references, captions and main-text claims.
The prior stored drafts and canonical figures remain immutable. A figure
inventory without corrected output is not completion.

Move lengthy withdrawal boxes, review-token explanations and internal status
language to the correction ledger or numerical appendix. The main text should
state the surviving result clearly; preserving an archive does not require the
paper to read as a transcript of its editorial history. Maintain the title,
authors and reach/dimension/recovery organization.

## 6. Publication scope and stopping rule

The current working scope is a finite-model information/recovery paper with
explicit limitations. For THAT candidate, the two deferred physical workstreams
are not automatically launch prerequisites if their unestablished claims are
not made. Conversely, publishing a strong continuum-acquisition or physical
order-resolution claim requires the relevant new evidence. Do not describe those
two workstreams as both 'deferred' and unavoidable blockers to every possible
finite-model manuscript. Author scope approval remains necessary; no acceptance
by any journal is promised.

No more interpolation search, comparator replay, hull replay or corrected-measure
R2 spectrum is requested by this review. The broader hypothesis has not been
rejected by an editorial dependency, and previously accepted limited results
are not revalidated under new assumptions merely by changing prose.

Authorize a bounded manuscript039 publication-preparation pass: incorporate the
two source-resolved methods; either locate eta from genuine records or retain its
specific limitation; finish relevant bibliography verification and the corrected
figures; reconcile matrix/dependency/document identities; produce an editable
source plus a readable rendered candidate and one honest remaining-gap report.
No more general method rewrites or new schemas are required. If a claimed
required input is genuinely unavailable, identify its exact affected claim and
stop with that limitation, rather than fabricating it or repeatedly declaring
all pins complete.

Return PUBLICATION_CANDIDATE_039_READY_FOR_AUTHOR_REVIEW or
PUBLICATION_CANDIDATE_039_BLOCKED. A ready token is not authorization to submit
or start R3B. It requires the actual rendered candidate, not only a plan/status
report. Convention-B spend remains 19146, unspent 854; boundary spend remains
33410. No new physical budget is authorized. Numerical-suite replay is not
required for document work. Preserve every earlier artifact and test outcome.

## Evidence pointers

At reviewed head unless stated otherwise:
- manuscript038 draft, selected claim matrix entries and pin/figure-status records.
- R1_20260826T022951Z_2ba66f02.json and HMT2M_20260828T064757Z_2ba66f02.json
  under artifacts/manifests/ identify the two execution commits.
- scripts/run_r1_main.py at the R1 execution commit and current head;
  src/phrt/metrics/scoring.py and artifacts/configs/R1_MAIN_FREEZE.json at R1 execution.
- scripts/run_hmt2_sealed_main.py, scripts/run_hmt2_sealed_main_score.py and
  scripts/run_hmt1_score.py, compared with their HMT2 execution-commit blobs.
The method note gives exact identities and selectors. External-source work is
separate and scoped explicitly in the core bibliography audit. The full supplied
42-page PDF was not re-extracted or newly hashed in this review; its already
provided content remains source history, not the new evidence completion record.
