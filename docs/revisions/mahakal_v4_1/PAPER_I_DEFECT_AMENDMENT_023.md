# PAPER_I_DEFECT_AMENDMENT_023

Status: AUTHORIZED_CORRECTION_CAMPAIGN; not a new submission freeze or a report of completed experiments.
Campaign: MAHAKAL_REVISION_INFORMATION_BOUNDARY_V4_1.
Repository: internalerror404/photon-ring-tomography.
Delivery branch: research/hmt2_resolution_aware_feature_measure_v0.
Read/append base: 7961e5bd7d46be23885151afa7664596cc58cfa6.
Base tree: 2425d64452dd3a02afe2b2cfe9e112513de7e557.

## 1. Decision and precedence

Accept the source-level R0 findings supplied in the author conversation, including the extension of index pairing to DELAY_ONLY and SPATIAL_ONLY and the correction to the production normalization repair target. Commit this record before execution.

Freeze 022 remains the immutable record of the submitted repository version. Its status fields, thirteen deliverables, canonical tables, banks, thresholds, hashes, and literal gate outcomes are NOT rewritten. Its restriction on further experiments is superseded only for the NEW R0-R2 campaign specified here. Items 7 and 10 do not prohibit this additive correction record; they continue to prohibit silently altering the old evidence.

This amendment authorizes: (a) completion of R0 against all twelve ledger items; (b) versioned R1 implementation and integration tests; and (c) one R2 nuisance-adjusted information audit at spin 0.5, inclination 50 degrees, after its prerequisites pass. This is project authorization under the author's direction, not a journal referee ruling.

There is no authorization here for a new reconstruction bank, retuning archived estimators, common-sky physical results, a 12-geometry NEW nuisance sweep, geometry mismatch, VLBI, learned reconstruction, or a new submission freeze. The inexpensive stored-curve normalization audit over all archived E3C geometries is part of R1, not a new geodesic campaign.

## 2. Formal dispositions

- C05a: UNRESOLVED_IMAGE is a cardinality-matched INDEX_SUM_CONTROL. Its numbers remain valid for that linear map; physical common-sky interpretation is withdrawn pending a registered acquisition model.
- C05b: DELAY_ONLY and SPATIAL_ONLY are INDEX_PAIRED_DELAY_SUBSTITUTION and INDEX_PAIRED_SPATIAL_SUBSTITUTION. Both are counterfactual index-paired controls, not verified co-registered interventions. Preserve the reported 0.98/0.57 comparison with that qualification; do not promote it to a coordinate-independent physical decomposition. The flat-probe identity remains an identity, not independent mechanism evidence.
- PAIRING_DESTROYED remains an intentionally unphysical permutation control. This finding neither invalidates its arithmetic nor makes it a physical history.
- C06: E3C TOTAL_FLUX retains order labels (three times eight = 24 rows); HMT2 TOTAL_FLUX explicitly sums orders and pixels (eight rows). Only the former receives the ORDER_RESOLVED_FLUX_CONTROL alias. HMT2's all-order flux result survives this diagnosis under the declared contrast and noise model.
- C04: marginal-only covariance is insufficient for general overlapping multi-output mixing. Identity and single-output-sum mixers used in the inspected archived arms are not invalidated on this ground. Full-covariance support is required before any extension.
- C11: HMT2's primary analytic signal is the fluctuation field, with the m=0 reconstruction block excluded. State absent/perfectly removed background explicitly. A data-estimated unknown background is a new analysis, not an achieved result.
- C02: the physical normalization repair must reach runner-level sqrt(mean(clean**2)) computations. Changing NoiseModel.from_snr alone does not repair the physical campaign. Cross-arm sharing of sigma in each geometry remains correct. Separate replay invariance, cross-geometry count dependence, and future refinement exposure.

## 3. Approved correction text

Use this meaning in a NEW manuscript/claim overlay, not by modifying the frozen files:

"At the declared operating point, ideal order-labeled data reduce aggregate contrast-morphology error relative to the direct arm. The tested all-order total-flux readout does not reproduce that material improvement. The cardinality-matched index-sum control is also nonmaterial, but it is not a co-registered unresolved image. The experiment therefore does not establish that a physical unresolved spatial image would fail, or that order labels rather than retained spatial information alone explain the gain."

For the archived 23.7% figure:

"The archived index-sum control retains 23.7% of the resolved old-age sensitivity volume under its declared indexing and noise convention. This is not a measured retention fraction for a physical unresolved detector."

For the mechanism split:

"The delay and spatial substitution comparisons are index-paired counterfactual ablations. Their numerical contrast is conditional on that pairing and does not establish a co-registered physical delay-versus-spatial decomposition."

R0 must locate EVERY occurrence, not only the four positions reported by the agent: abstract, scope, mechanism section, attribution section, figure captions, conclusion, evidence/claim ledgers, and derivative releases. Repository and uploaded-PDF section numbers differ. Store old text, proposed text, path/claim ID, hash, and disposition in an additive overlay. Do not edit the old evidence ledger in place.

## 4. Normalization materiality

At fixed clean reference response E=||A_unit j_ref||^2, legacy information is proportional to the direct row count m_g. For the E3C common-count convention m*=1536*8, the count-only corrected/legacy information multiplier in a000_i020 is 1536/1483 = 1.035738368172623; sigma_new/sigma_old = sqrt(1483/1536) = 0.9825959452728606. Other physical differences between geometries are not explained by this ratio.

This does not invalidate within-geometry arm comparisons. Nor does a small multiplier prove that a quantized endpoint is unchanged. R1 must rescale stored curves/singular values analytically, rebuild all threshold masks and interval endpoints, and report every crossing. Recompute log(1+I) integrals rather than multiplying J_old by the information multiplier. No new ray tracing is needed for this count-only test.

## 5. Preservation and exit rule

New outputs go under artifacts/revisions/mahakal_v4_1/<run_id>/; new source/runners/tests use revision_v4_1 namespaces. Reuse authenticated frozen maps. Never overwrite an existing output, regenerate a historical freeze to match changed bytes, turn an old FAIL into PASS, or label an old bank fresh.

The first return must distinguish source-confirmed dispositions, completed tests, pending physical results, and blockers. A zero conditional spectrum is a scientific outcome. An unresolved numerical-rank decision is NUMERICALLY_UNRESOLVED, not a favorable gate outcome. Stop after the reference-geometry R2 report.

Read order: this amendment; CORRECTION_LEDGER.md; AGENT_HANDOFF.md; EXPERIMENT_PROTOCOL.yaml. These four files are self-contained for R0-R2. No client-local download, external working draft, or missing helper script is a prerequisite.
