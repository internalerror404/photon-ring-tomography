# Photon-Ring Retarded-Time Tomography: revision 044

Complete Paper I revision integrating the order-attribution triage and the cached coarse/core/fine all-ray sensitivity check. It is an additive revision of manuscript042/043; all earlier directories remain unchanged.

The paper now states that the first indirect order is the load-bearing historical channel, the direct order improves nuisance rejection, order 2 is not required for the two operational modes, and the operational count remains two across the available cached screen-sampling ladder. It also states that the exact two-mode subspace moves more than the count, and that no cached level bounds the continuum acquisition.

Compile the self-contained source with:

```sh
latexmk -pdf -interaction=nonstopmode -halt-on-error Photon_Ring_Retarded_Time_Tomography_044.tex
```

The complete modular source package and rendered PDF are delivered with the author-facing artifact package. No scientific simulation or physical query is required to compile this manuscript. Final submission remains an author decision.
