# Superseded

This directory holds a two-level execution of
`scripts/revision_v4_1/t042_core_fine_feasibility.py` (ARCHIVED, CORE_ALL,
FINE_ALL), run before the coarse level was added to the ladder.

It is superseded by `F042B_20260910T220443Z_391c23c`, which adds COARSE_ALL, the
convergence trend and the Weyl bound, and reproduces every number here
exactly. Kept rather than deleted so the record shows what was run and when.
